{
  "name": "solana-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npx esbuild index.js --bundle --minify --outfile=index.min.js"
  },
  "author": "",
  "license": "ISC",
  "browser": {
    "stream": "stream-browserify"
  },
  "dependencies": {
    "@solana/wallet-adapter-base": "^0.9.5",
    "@solana/wallet-adapter-wallets": "^0.15.5"
  },
  "devDependencies": {
    "esbuild": "^0.14.27",
    "npx": "^10.2.2",
    "stream-browserify": "^3.0.0"
  }
}
